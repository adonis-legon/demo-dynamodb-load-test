# Local Development Environment Configuration
# This file contains configuration for local development with LocalStack

# AWS Configuration
AWS_REGION=us-east-1
# For host scripts (setup, deploy, etc.) - use localhost
AWS_ENDPOINT_URL=http://localhost:4566
# For containerized app - use Docker host gateway
AWS_ENDPOINT_URL_CONTAINER=http://host.docker.internal:4566

# AWS Credentials (dummy values for LocalStack)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# CloudFormation Stack Configuration
STACK_NAME=dynamodb-load-test-local
TEMPLATE_S3_BUCKET=local-cloudformation-templates
TEMPLATE_S3_KEY_PREFIX=dynamodb-load-test/

# Application Configuration
TABLE_NAME=local-load-test-table
CONCURRENCY_LIMIT=20
TOTAL_ITEMS=500
MAX_CONCURRENCY_PERCENTAGE=100.0
DUPLICATE_PERCENTAGE=15.0
CLEANUP_AFTER_TEST=true

# DynamoDB Configuration
READ_CAPACITY_UNITS=5
WRITE_CAPACITY_UNITS=10

# Local Development Settings
ENVIRONMENT=local